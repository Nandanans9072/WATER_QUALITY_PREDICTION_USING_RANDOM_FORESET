# -*- coding: utf-8 -*-
"""Water Quality Prediction.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1AoEqApaSt-rL4sBJkP7cRqk08jMzgUDt
"""

from google.colab import files
uploaded = files.upload()

import pandas as pd

df = pd.read_csv(list(uploaded.keys())[0])  # Automatically loads the uploaded file
df.head()

import pandas as pd
import numpy as np
import seaborn as sns
import plotly.express as px
import matplotlib.pyplot as plt


from sklearn.preprocessing import StandardScaler
from sklearn.model_selection import train_test_split

df = pd.read_csv("/content/water_potability.csv")
df

df.columns

df.describe()

df.info()

df.isnull().sum()

plt.figure(figsize=(12,8))
sns.heatmap(df.corr(),annot=True)

df.head()

x = df.drop("Potability",axis=1)
 y = df["Potability"]

x.shape , y.shape

scaler = StandardScaler()
x = scaler.fit_transform(x)
x

x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=0.2)

x_train.shape , x_test.shape

from sklearn.ensemble import RandomForestClassifier
from sklearn.metrics import accuracy_score
from sklearn.metrics import confusion_matrix

#Creating model object
model_rf = RandomForestClassifier()

#Traing Model RF

model_rf.fit(x_train,y_train)

#Making Prediction
pred_rf = model_rf.predict(x_test)

accuracy_score_rf = accuracy_score(y_test, pred_rf)
accuracy_score_rf * 100

cm3= confusion_matrix(y_test,pred_rf)
cm3

import pandas as pd

# Example new input
new_data = [[7.0, 150, 200, 30, 10, 30, 3.5, 250, 10]]

# Convert to DataFrame with correct feature names
new_data_df = pd.DataFrame(new_data, columns=['ph', 'Hardness', 'Solids', 'Chloramines', 'Sulfate', 'Conductivity',
       'Organic_carbon', 'Trihalomethanes', 'Turbidity'])

# Scale using the same scaler
new_data_scaled = scaler.transform(new_data_df)

# Predict
prediction = model_rf.predict(new_data_scaled)

if prediction[0] == 1:
    print("Water is GOOD (Potable)")
else:
    print("Water is BAD (Not Potable)")